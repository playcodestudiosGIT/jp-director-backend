
const { response } = require('express');
const {  } = require('mongoose').Types;

const {  } = require('../models');

const coleccionesPermitidas = [
    'usuarios',
    'categorias',
    'productos',
    'roles'
];



const addEvent = async( termino = '', res = response ) => {

     

}


module.exports = {
    addEvent
}